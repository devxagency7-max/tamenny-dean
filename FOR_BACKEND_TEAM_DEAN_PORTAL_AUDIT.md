# تقرير فحص شامل — بورتال عميد الكلية (Faculty Dean Portal)
**التاريخ:** 2026-09-15
**النطاق:** فحص كامل لربط `tamenny-dean-main` بالباك إند الحقيقي (`http://204.168.149.185/api/v1`) طبقًا لـ `FACULTY_DEAN_AND_INTERN_BACKEND_SPEC.md` v2.1.0.

---

## 1. الخلاصة السريعة

| البند | الحالة |
|---|---|
| كل الـ 21 endpoint الخاصة بالعميد (Part 9 من السبك) | ✅ موجودة وشغالة (بترجع 401 مش 404 بدون توكن) |
| CORS للدومين الحي `https://tamenny-dean-main.vercel.app` | ✅ مُفعّل بالفعل على الباك إند |
| مشكلة Mixed Content (HTTPS↔HTTP) | ✅ اتحلت (proxy عن طريق `vercel.json`) |
| أخطاء فرونت إند حقيقية كانت بتمنع الربط الصحيح | ✅ **6 أخطاء تم اكتشافها وإصلاحها** في نفس الجلسة (تفصيل في القسم 3) |
| فجوات حقيقية في الباك إند لازم نطلبها | ⚠️ **5 نقاط** محتاجة رد/تعديل من تيمكم (تفصيل في القسم 4) |
| اختبار end-to-end بحساب عميد حقيقي | ⚠️ لم يتم — نحتاج بيانات دخول اختبارية (تفصيل في القسم 5) |

---

## 2. منهجية الفحص

بما إننا مش معانا حساب عميد حقيقي (إيميل/باسورد) للاختبار المباشر، تم الفحص على 3 مستويات:

1. **فحص وصول الـ endpoints (Reachability):** استدعاء كل endpoint بدون توكن والتأكد إن الرد `401` (موجود ومحتاج صلاحية) مش `404` (مش موجود) أو `500`/`network error`.
2. **فحص شكل الاستجابة الحقيقي:** مطابقة الـ headers (`Access-Control-Allow-Origin`, route constraints) مع المتوقع من السبك.
3. **اختبار وحدة (Unit Test) لمنطق التطبيع (Normalization):** تغذية دوال الـ frontend بأمثلة الاستجابة الموثقة *حرفيًا* في السبك، والتأكد إن الناتج مطابق لما تتوقعه الواجهة.

---

## 3. أخطاء Frontend تم اكتشافها وإصلاحها في هذه الجلسة ✅

هذه أخطاء كانت **في كودنا احنا**، مش في الباك إند — بس بنوثقها هنا عشان الشفافية الكاملة، ولأن بعضها مرتبط بفهمنا لشكل الـ response:

### 3.1 أسماء query params غلط في `GET /dean/interns`
كنا بنبعت `?status=active&pharmacy=X` بينما السبك بيحدد `?trainingStatus=InTraining&pharmacyId=<guid>`. النتيجة: أي فلترة أو بحث كان هيتجاهله الباك إند بصمت ويرجع القايمة كاملة. **تم الإصلاح** بإضافة دالة تحويل من قيم الواجهة المحلية (`active/completed/pending/risk`) لقيم الـ enum الحقيقية (`InTraining/Completed/Enrolled/Inactive`).

### 3.2 عدم إرسال `pageSize`
كنا مش بنبعت `pageSize` خالص، يعني أي كلية عندها أكتر من 20 متدرب (الافتراضي حسب السبك) كان هيظهرلها بس أول 20. **تم الإصلاح مؤقتًا** بطلب `pageSize=200` كحل عملي لحد ما نبني واجهة pagination حقيقية (لو متوقع كليات بأكتر من 200 متدرب، محتاجين نبني الـ pagination UI فعليًا — قولولنا العدد المتوقع الأقصى).

### 3.3 بيانات الجامعة/الكلية/اسم العميد كانت hardcoded في أماكن متعددة
شريط الكلية بالسايد بار، صفحة "ملف الكلية"، وحقل "المؤسسة" بصفحة البروفايل — كلهم كانوا نص ثابت في الـ HTML مش متصلين بـ `/dean/me` خالص. ده سبب ظهور "جامعة القاهرة / كلية الصيدلة / أ.د. خالد السيد إبراهيم" حتى لو دخل عميد حقيقي تاني (زي ما لاحظتوا مع حساب `abdallahmfathy204@gmail.com`). **تم الإصلاح** — كل العناصر دي بقت مربوطة ديناميكيًا بنتيجة `/dean/me`.

### 3.4 عداد المتدربين بالسايد بار وأرقام فلاتر الداشبورد كانت ثابتة
"148" جنب "سجل المتدربين"، و"(148)"/"(126)"/"(18)"/"(4)" في فلاتر الداشبورد — كل ده كان نص ثابت مش بيتحدث من `/dean/dashboard/summary`. **تم الإصلاح**.

### 3.5 `targetHours` و `status` كانوا hardcoded في تطبيع بيانات ملف المتدرب
دالة تطبيع `GET /dean/interns/{id}` كانت بتحط `targetHours: 300` ثابت بدل قراءته من `progress.targetHours`، وبتحط `status: 'active'` ثابت (لأن الـ endpoint ده — لاحظنا — مبيرجعش حالة التدريب أصلاً، شوف البند 4.2). **تم الإصلاح جزئيًا**: `targetHours` بقى بيتقرا من الاستجابة الحقيقية، و`status` بقى بياخده من صفحة القايمة (roster) اللي فيها الحالة فعلاً، كحل مؤقت لحد ما تضيفوا الحقل في تفاصيل المتدرب (البند 4.2).

### 3.6 `GET /dean/me` مكنش بيتنادى تاني بعد أول تسجيل دخول
لو الملف الشخصي المخزن محليًا اتفقد أو حصل فيه أي مشكلة، الواجهة كانت هتفضل عارضة بيانات افتراضية للأبد بدون أي محاولة تحديث. **تم الإصلاح** بإضافة تحديث تلقائي في الخلفية بعد كل فتح للتطبيق.

---

## 4. فجوات وأسئلة محتاجة رد من تيم الباك إند ⚠️

### 4.1 مفيش endpoint لعرض تقييمات المشرف الشهرية لمتدرب معين
جدول `TrainingEvaluations` موجود في تصميم قاعدة البيانات (Part 10.1، جدول #9) وموثق في Part 3.2 كجزء من الـ Dossier، لكن مفيش أي endpoint في الكتالوج النهائي (Part 15) بيرجع تقييمات متدرب معين للعميد. محتاجين مثلاً:
```
GET /api/v1/dean/interns/{id}/evaluations
Required Role: FacultyDean
```
بنفس شكل باقي الاستجابات (`{success, data: {items: [...]}}`)، وكل عنصر يحتوي `clinicalKnowledgeScore, communicationScore, ethicsAndDisciplineScore, overallScore, supervisorComments, evaluatedAt` (نفس حقول جدول `TrainingEvaluations`).

### 4.2 `GET /dean/interns/{id}` مفيهوش حقل لحالة التدريب
الـ endpoint الخاص بتفاصيل متدرب واحد (Module 9.2) بيرجع `profile`, `placement`, `progress`, `documents` — لكن مفيهوش `trainingStatus` (اللي موجود في endpoint القايمة `GET /dean/interns` بس). النتيجة: صفحة الملف الكامل للمتدرب مش عندها مصدر مستقل لمعرفة هل هو "نشط/مكتمل/متابعة" لو اتفتحت الصفحة مباشرة (رابط محفوظ مثلاً) من غير المرور بصفحة القايمة الأول. الرجاء إضافة `trainingStatus` لاستجابة الـ endpoint ده كمان.

### 4.3 تأكيد: هل `POST /users/sync` مطلوب قبل أول استخدام لـ `GET /dean/me`؟
فهمنا من Part 8.5 إن حساب العميد بيتعمله provision كامل من الأدمن (Firebase + AspNetUsers + FacultyDeans) قبل أي تسجيل دخول، يعني السجل جاهز بالكامل من غير الحاجة لخطوة "sync" زي المرضى/الصيادلة اللي بيسجلوا نفسهم. فرونت إند العميد حاليًا **بيتخطى** استدعاء `/users/sync` تمامًا وبينادي `/dean/me` على طول بعد نجاح Firebase. ممكن تأكيد إن ده صح فعلاً ومفيش مشكلة لو أول تسجيل دخول لعميد جديد اتعمله provision لسه ما سجلش دخول قبل كده؟

### 4.4 تأكيد اسم الحقل الراجع من `GET /dean/me`: `title` ولا `academicTitle`؟
طلب إنشاء العميد (`POST /admin/deans`) بياخد الحقل باسم `academicTitle`، لكن مثال استجابة `GET /dean/me` في نفس المستند (Module 9.1) بيرجع الحقل باسم `title`. إحنا الفرونت إند دلوقتي متوقعين `title`. ممكن تأكيد الاسم الفعلي اللي بيترجع في الاستجابة الحقيقية؟

### 4.5 [أولوية عالية] طلب جديد: العميد يعمل provision لحساب مشرف (Supervisor) جديد بالكامل
بعد نقاش داخلي، اتضح إن الموديل المطلوب مختلف عن اللي في Part 6 و 8.5 من السبك:

- **مش** "صيدلي مرخّص موجود بيتعمله ترقية/ربط" (زي `POST /api/v1/admin/supervisors` الحالي اللي بياخد `userId` لصيدلي موجود بالفعل، ومحصور على `Admin` role).
- **المطلوب:** العميد (`FacultyDean`) نفسه يقدر ينشئ **حساب مشرف جديد بالكامل** (اسم + إيميل + باسورد، بالظبط زي منطق `POST /api/v1/admin/deans`)، مربوط بكليته هو تحديدًا، عشان يشرف على متدربي كليته بس.
- العميد **مالوش أي علاقة بالصيدليات أو الصيادلة الموجودين فعليًا في النظام** — نطاقه محصور في متدربين كليته والمشرفين اللي هو نفسه بيعينهم عليهم.

محتاجين منكم:
1. تأكيد هل ده تغيير معماري مقبول (يعني الـ Supervisor في السياق ده مش لازم يكون `Pharmacist` role حقيقي مرتبط بصيدلية فعلية، لكن دور أكاديمي إشرافي مستقل)؟
2. لو موافقين، endpoint جديد مقترح:
   ```
   POST /api/v1/dean/supervisors
   Required Role: FacultyDean
   Body: { name, email, password, phone? }
   ```
   بيتصرف زي `POST /admin/deans` بالظبط لكن الـ Role الناتج `Supervisor` (أو الاسم اللي تفضلوه) ومربوط تلقائيًا بـ `FacultyId` بتاع العميد نفسه (مش بيتبعت في الـ body، بيتاخد من التوكن).
3. `GET /api/v1/dean/supervisors` — لسرد المشرفين اللي عملهم العميد ده بالذات.
4. توضيح: إيه اللي المشرف ده هيشوفه/يقدر يعمله بعد تسجيل الدخول؟ (نفترض: قايمة متدربين كليته المُعيّنين عليه، مراجعة الـ clinical drafts بتاعتهم، تسجيل التقييمات — زي المتفق عليه في Part 6.1، لكن بدون الحاجة لكونه صيدلي حقيقي في صيدلية).

---

## 5. ملاحظة مهمة: الاختبار الكامل (End-to-End) لسه محتاج بيانات دخول حقيقية

كل الفحص اللي اتعمل في التقرير ده كان بدون توكن صالح (فحص وصول الـ routes + اختبار منطق التطبيع بأمثلة من السبك نفسه). **محتاجين واحد من الاتنين** عشان نقفل التأكد الكامل:
- بيانات دخول حساب عميد حقيقي (إيميل + باسورد) للاختبار المباشر على الموقع المنشور، أو
- تأكيد إن أي حساب عميد اتعمله provision جاهز نجرب بيه فورًا بمجرد ما تدونا الباسورد المؤقت.

---

## 6. للمرجعية: نتائج فحص الـ Endpoints الفعلية (بدون توكن)

```
GET  /dean/me                              -> 401 ✅ (موجود)
GET  /dean/interns                         -> 401 ✅
GET  /dean/interns/{valid-guid}            -> 401 ✅ (لاحظنا: لازم الـ id يكون GUID صحيح الصيغة، وإلا بيرجع 404 بسبب route constraint — سلوك متوقع، مش خطأ)
GET  /dean/interns/{id}/clinical-operations-> 401 ✅ (مع GUID صحيح)
GET  /dean/interns/{id}/activity-logs      -> 401 ✅ (مع GUID صحيح)
GET  /dean/interns/{id}/chats              -> 401 ✅ (مع GUID صحيح)
GET  /dean/dashboard/summary               -> 401 ✅
GET  /dean/dashboard/activity-feed         -> 401 ✅
GET  /dean/dashboard/internship-progress   -> 401 ✅
GET  /dean/partner-pharmacies              -> 401 ✅
POST /users/sync                           -> 401 ✅ (مع POST method)
GET  /users/me                             -> 401 ✅
GET  /admin/universities                   -> 401 ✅
GET  /admin/faculties                      -> 401 ✅
GET  /admin/deans                          -> 401 ✅
GET  /admin/supervisors                    -> 401 ✅
GET  /supervisor/drafts                    -> 401 ✅
POST /supervisor/evaluations               -> 401 ✅ (مع POST method)
```

كل الردود فيها `Access-Control-Allow-Origin: https://tamenny-dean-main.vercel.app` ✅ لما بنبعت `Origin` header مطابق، يعني الـ CORS متظبط صح من ناحيتكم.

---

**الخلاصة:** البورتال دلوقتي متصل بشكل صحيح تقنيًا بكل endpoints العميد الموجودة، وكل أخطاء الفرونت إند اللي كانت بتمنع ظهور البيانات الحقيقية اتصلحت. الباقي عليكم: توضيح الـ 5 نقاط في القسم 4 (خصوصًا 4.1 و4.2 و4.5)، وتوفير حساب اختبار حقيقي عشان نقفل التأكد النهائي end-to-end.
